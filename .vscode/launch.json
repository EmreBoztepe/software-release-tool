{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run GUI",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/src/gui/gui.py",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}
